基于TCP协议相较于OSPF(仅基于IP)和IS-IS(基于数据链路层,天然支持IP协议),更加可靠
IGP需要快速发现邻居和收敛,TCP太慢了
只传递路由信息,不传递内部拓扑,只传递AS路径,AS内部不暴露
触发式更新,而不是周期式更新,消耗资源更少

日常传递的包组成部分包含:目的网段,经过的as_path,下一跳路由器
路由信息优选：通过路径属性

BGP的发言者：发送BGP报文的路由器
对等体：两个发言者(ip要可达)交换BGP路由表，建立邻居关系可以跨越路由器(可以不是直连路由器)，因为是基于TCP协议，因此IBGP可以做到两两路由器全互联
对等体建立过程中会建立2个TCP连接(基于179端口号)，但只使用一个，建立IBGP时TCP连接的接口一般使用出接口或loopback，建立使用loopback，可以快速检测+快速切换，首要目的是传递路由信息
因为出接口是物理端口，一旦光模块down了，对应的ip就不可达，而loopback接口是逻辑端口，只要还有一个物理端口up，就可以动态切换到其他正常链路
建立EBGP时通常使用直连接口，虽然这个是down掉就不可达，但是其想要效果就是快速检测故障，加速路由收敛，从而避免更严重的问题(首要目的是保证安全)
因为EBGP可能终端，所以两个AS间会建立多个EBGP会话

头部相同部分：
16B的Marker，用于BGP验证的计算
2B的Length，表示BGP消息总长度(包含报文头在内)，长度范围是19~4096
1B的Type，表示BGP消息类型，Type有5个可选值

最开始会发送open报文，包含BGP标识符，也就是router id，通过此确定使用哪一个TCP连接,选择router id较大的路由器发起的TCP连接，另一个TCP连接会被关闭
会发送多个open报文协商参数，是TCP建立后的第一个报文
版本号、本地AS号（确定BGP是I还是E）、keep-alive保持时间(默认180s)(发送间隔默认是1/3的时间)，BGP的ID,可选项

然后通过update报文发送路由更新信息
发布通过NLRI字段(不固定长度）(network layer reachable infomation)(网络层可达信息)，撤销通过withdrawn Routes字段(不固定长度)

Notification：报告错误信息(具体类型等)，终止对等体信息

Keep-alive：维持对等体关系，只包含报文头部,不包含其他字段

Route-Refresh：在改变路由策略后请求对等体重新发送路由信息，用来重新学习对等体的路由信息，并非所有设备都支持该类报文


BGP邻居状态(出现错误会回到Idle态)
1、准备TCP连接 Idle(缺乏对等体的路由器会处于该状态，路由表中没有对应ip)
2、正在尝试进行TCP连接 Connect
3、TCP连接没建立成功，反复尝试TCP连接 Active 若连接重传定时器超时，会又回到Connect状态。1(若179端口号被禁用，可能出现TCP连接失败)
4、TCP连接建立成功，发送Open包，进行参数协商 OpenSent
5、协商成功，自己发送Keepalive包，等待对方keepalive包 OpenConfirm
6、协商一致，且互收keepalive包，开始用update报文同步路由信息，建立邻居关系 Established

BGP重要表：
BGP对等体表（本机和邻居信息)
BGP路由表(目的网段,下一跳,)
*代表有效，>表示最优
会罗列多条路径，可以确定哪条最优(通常由IGP路由表的路由条目注入BGP路由表)
Network命令行注入，然后通过update报文传递，精细化把控路由信息
import-route方式注入路由方式：可以批量注入大量路由

update时只传递聚合路由
传送路由信息原则：
1、只将自己最优路由发给邻居
2、从EBGP获得的路由会传送给所有对等体(无所谓I还是E)
3、从IBGP对等体获得的路由不会发送给其他IBGP对等体(也称为IBGP水平分割)，可以避免A端断连后和A端相连的B、C端无线循环互相加跳数
4、一台路由器获得的IBGP路由，不会传给EBGP对等体(只有IP路由表(相较于BGP路由表更底层，更物理的连接)内有去往该即将传递路由的路由，才会通过EBGP的方式传输)(产生BGP关系时,有某几台设备没有运行BGP协议,导致没有去往其他BGP的目的路由，所以数据传给这边，找不到目的路由，就会丢失数据，产生路由黑洞)
但是第4条原则不解决通与不通的问题，所以某些设备不支持
路由反射
MPLS可以解决

update包含Withdrawn Routes Length、Withdrawn Routes、Total Path Attribute Length(分别指出后两条的格式)、Path Attributes、Network Layer Reachability Information（NLRI）
新增/撤回的路由列表是 按顺序给出10进制的掩码长度、16进制的掩码长度、后面接着实际掩码的16进制(NLRI和withdrawn routes)
两个length字段为0，则说明没有要传递的信息
路由属性中每一个单元都是TLV三元组
