ip as-path-filter 2 permit 200_300

定义了一个 AS 路径过滤列表，编号为 2。

200_300 是正则表达式：

_ 代表 AS 路径中的分隔符（相当于单词边界）。

意思是：AS 路径中先出现 200，紧接着出现 300（例如 200 300 或 200 300 400）。

注意：它不会匹配 300 200 或 2000 300。

route-policy test permit node 10

创建路由策略 节点名为test,序列号为10

if-match as-path-filter 2

该路由策略中调用上面第 1 步定义的过滤列表 2。

含义：如果路由的 AS 路径满足“包含 200 紧接着 300”，则匹配成功。
