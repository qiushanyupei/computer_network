TCP协议理论：
1、通过socket机制（sip,dip,sport,dport）实现多路传输，在sip,dip确定时通过不同port传输数据包
2、三次握手：payload部分通常为0
1）A->B,SYN：A的序列号
2）A<-B,SYN：B的序列号，ACK：A的序列号+1
3）A->B,SYN：接收到包的ACK，ACK:B的序列号+1

四次挥手：（也可能是服务端先挥手，看谁的数据先传完）
第一次挥手（客户端->服务端）：FIN+ACK+序号
第二次挥手（服务端->客户端）+中间N多次第三次挥手
通用规则：序列号用对方的确认号，确认号用对方的序号+1，但是第三次挥手通常序列号、确认号和第二次挥手相同
等待所有数据发送完毕，会发送FIN+ACK表示结束
客户端->服务端（第四次挥手）
规则照旧，发送ACK（知道你发完了，结束吧）

TCP四次挥手后最后收到FIN消息的一方发出ACK后进入 TIME_WAIT 状态，等待 2MSL（Maximum Segment Lifetime，最大报文生存时间）：
1）确保对方收到最后一次ACK，防止对方超时重传
2）让网络中残留的旧数据包消失（防止对新连接（同一个ip和端口组合再次连接）造成干扰）

TCP四次挥手并不会在关掉浏览器页面时立即执行，浏览器仅是应用层协议HTTP链接的，TCP连接一般是通过超时机制关闭

TCP常见报头含义分为：正常报文、keepalive、Duplicate ACK、重传、out-of-order、window update（wireshark自动识别）
Duplicate ACK：告知发送方收到乱序的包，后面的数据包暂存
1）触发快速重传，重复发送一个序号的ACK，发送方收到3次后认为丢包，触发快速重传（配合SACK）
2）确实重传了，提示服务端（配合D-SACK）
wireshark判断重传方法：同一TCP流中序列号相同的包
TCP keepalive报文特征：
1）Len必须为0或1字节，为了探测对端是否存活而设计的小数据包
2)当前的 Seq Num 必须恰好等于“下一个期望序列号 – 1”。这在效果上相当于序列号回退了1个字节，是一个特殊的、在窗口之外的值，旨在强制对端回复ACK进行确认

确认应答机制+超时重传机制：发送方发数据，接收方确认后，发送方才能继续发（滑动窗口机制可以同时发多个），否则超时重传（效率较低）

TCP缓冲区：处于操作系统内核区域，逻辑上表现为环形队列，底层常由链表实现
发送方通过socket（send函数）或其他方式把要发送的数据包存入TCP缓冲区
接收方也是调用内核（如socket recv函数）获取接收缓冲区数据

TCP缓冲区窗口：包含已发送没有收到ACK、可以发送但还未发送的数据。收到接收方ACK后窗口左边界和右边界都右移，可以发新的包了(收到左边界右侧的包的确认，即说明左边的也收到了，所以可以左移到该包右侧)
客户端、服务器的rwnd（receive window）可以不同,只影响各自方向的速度。（家庭网络中上下行速度不同主要是由于ISP服务限制）

TCP协议头：
sport(2字节）、dport（2字节）
TCP Segment Len是wirehsark自动计算的data部分长度、seq_num(4字节)、ack_num(4字节)
Conversation completeness是对于wireshark对于当前TCP会话的信息总结
header length(TCP头)(4bit)（以4字节为计算单位）、Reserverd(6bit)（可能低三位有其他作用）、其他标志位、window(2字节)、checksum(2字节)（校验TCP头+payload）、Urgent Pointer（2字节）（URGFlag置1时有效，指出报文段中紧急数据字节数，紧急数据在报文段数据最前）、Options（可变最长40字节）、Padding(用来补位、保证TCP头部是4字节的整数倍)
Flags字段内部：
URG(1bit):紧急指针有效标识（相当于高优先级的数据）。 
ACK(1bit):确认序号有效标识。只有当ACK=1时确认号字段才有效。当ACK=0时，确认号无效。 
PSH(1bit):标识接收方应该尽快将这个报文段交给应用层。接收到PSH = 1的TCP报文段，应尽快的交付接收应用进程，而不再等待整个缓存都填满了后再向上交付。 
RST(1bit):重建连接标识。当RST=1时，表明TCP连接中出现严重错误（如由于主机崩溃或其他原因），必须释放连接，然后再重新建立连接。 
SYN(1bit):同步序号标识，用来发起一个连接。SYN=1表示这是一个连接请求或连接接受请求。 
FIN(1bit):发端完成发送任务标识。用来释放一个连接。FIN=1表明此报文段的发送端的数据已经发送完毕，并要求释放连接。
三次握手中Flags置位情况。1）Syn:Set；2）Syn,Ack:Set;3）Ack:Set

Options（三次握手时协商MSS必出现）中参数：（有1字节的kind决定Option类型）
Maximum Segment Size (MSS)（4字节）：TCP最大发送payload字节数，常为1460字节（kind+length+MSS Value(2字节)）
No-Operation (NOP)（1字节）：占位，无操作，用来padding为4字节的整数倍
Window Scale（3字节）：（kind+length+shift count）
实际窗口 = TCP头中窗口字段值 × 2^(shift count)
SACK Permitted(2字节):Kind(1字节)+length(1字节)；不发的话就说明不支持SACK(Selective Permitted)
SACK：用于说明收到了哪段数据，如果是D-SACK，说明是重复收到某数据段；如果ACK在SACK连续数据段前，则可以精确推断出哪些数据包没收到

[SEQ/ACK analysis]是wireshark自动计算得到
1）可以定位当前_ws.col.protocol == "TCP"报文是确认哪条报文的
2）确认的往返时间RTT (Round-Trip Time)
3）初始三次握手中得出的iRTT(initial RTT)

