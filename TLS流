从技术上看，TLS（传输层安全协议）工作在TCP协议之上，它接收应用层（比如你的浏览器）要发送的、可能是一段连续的数据，然后把这些数据加密后发给对方。这个持续不断的加密数据序列，就构成了一个"流"。

TLS流有几个关键特点：

像水管一样是连续的双向通道
数据会被"切块"发送：虽然TLS提供的是"流"的抽象，但在实际传输时，数据会被分割成一个一个的"记录"（TLS Record）来发送，每个记录最大为16KB。应用层的一次数据发送（比如HTTP请求），可能会被拆成多个TLS记录发送，也可能多个小数据被合并到一个记录里发送。接收方收到后会把这些"碎片"重新拼好，还原成完整的数据。

采用流加密算法时更直接
