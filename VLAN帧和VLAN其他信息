VLAN帧格式相对于EthernetⅡ帧，在smac和2字节的length/type之间插入4字节的802.1Q Tag；帧总长最长较Ethernet Ⅱ也多4字节，为1522字节。
802.1Q Tag组成部分：
1、Type(2字节)：固定为0x8100
2、PRI（Priority）（3bit）：0~7,值越大优先级越高、交换机阻塞时优先发送
3、CFI (Canonical Format Indicator)（1bit):表示MAC地址是否是经典格式（非经典是高位字节在右，和经典格式方向相反）。CFI为1兼容FDDI（Fiber Distributed Digital Interface）帧和令牌环网帧（几乎淘汰）。在以太网中，CFI的值为0。
4、VLANID（12 bit：0~4095）:只设置优先级，VLANid设置为0；4095是预留id，即只能创建1-4094个

每一个VLAN属于一个广播域

在交换机内配置VLAN步骤：
默认会有一个VLAN1

vlan类型：access trunk hybrid
业务vlan
管理vlan:网络管理的流量。大公司单独做一套单独物理网络来监控
互联vlan：三层设备之间需要配置直连网段，通过vlanif接口配置。因为两个三层设备就无法在同一个广播域内直接发数据包

所有创建设备多需要加上网管vlan

还需要配置接口的vlan类型，使接口能传递vlan流量
