路由表中会存储的信息
字段	                           含义	                             实际例子
目的网络/前缀	      要去的目的地（网络地址）	                   192.168.1.0/24
下一跳	              下一站路由器的IP地址（直连网络里没有）	     192.168.1.1 或 10.0.0.2
出接口	              从自己哪个物理/逻辑接口发出去	           GigabitEthernet0/0 或 ppp0
路由协议/来源	      这条路由是怎么学来的（静态、OSPF、直连等）	Static, OSPF, Direct, BGP
度量值	到达目的地的“代价”      （跳数、带宽等），用于选路	               2, 10
优先级/管理距离	        路由协议的“可信度”，越小越优先	          静态路由通常 1，OSPF 11

1）路由表中路由类型：直连路由、静态路由、动态更新路由、默认路由（常为0.0.0.0/0）
2）路由匹配规则：IP & 掩码 == 网络地址（匹配的路由中选择掩码最长的，因为最为精确）

路由表更新方式分为静态路由（管理员手动配置）和动态路由（通过距离矢量算法RIP，链路状态算法OSPF、IS-IS，路径矢量算法BGP）

RIP协议：度量依据跳数，路由器只知道邻居，不掌握全网拓扑。（大型、复杂网络环境中已淘汰）（最大跳数限制为15跳，无法应用于大型网络；更新路由表基于UDP，一旦出现坏消息丢失难以处理，收敛速度慢；每30秒就广播一次完整的路由表，在大型网络中会消耗大量带宽）
更新流程：
对于新获悉的路由，如果之前不知道，就直接加入路由表。
对于已知的路由，如果新路径的跳数更少，就更新为这条更优的路径。
对于已知的路由，如果新路径来自同一个邻居，无论跳数增加还是减少，都会无条件更新（这被认为是网络拓扑可能发生了变化）。

邻居发现：启动后通过广播或组播发送请求，发现邻居（必须在同一内网）。
定期更新：周期性地（实际上是30秒加上随机偏移量）将自己的整张路由表发送给直连邻居。（或更新网络拓扑后立即更新，但是更新的消息是基于UDP的可能丢失）

链路状态更新方式：度量延迟等。每台路由器独立了解完整网络拓扑，自行计算最优路径。（OSPF、IS-IS）
邻居发现与建立：通过Hello报文（基于IP协议）发现内网邻居并建立邻接关系。（这里的内网一般是指有多个路由器的校园网或者企业内网，普通家庭内网就一个路由器，用不到）
链路状态数据库同步：交换LSA（Link State Advertisement），确保所有路由器有一致的链路状态数据库。
运行SPF算法：利用该数据库（即完整地图），以自己为根运行最短路径优先算法，计算到每个目的地的最优树。
生成路由表：从SPF树中得到最佳路径，放入路由表。
更新周期：默认每30分钟重发LSA，链路状态出现变化时也会重发LSA
OSPF和IS-IS区别：IS-IS可以选择PPP协议跳过子网检查，在直连情况下支持不同子网建立连接；OSPF在直连情况下也不支持

基于路径矢量更新：BGP协议
与其他路由器建立TCP连接（相对于RIP、OSPF突破了只在内网中交换信息的限制），交换AS Path相关属性，选择最优路径更新路由表（As Path更短，local preference优先级更高）
AS列表具体呈现是每一个编号代表一个AS，可能形式为：(65001 64955 65003)
对于不同AS之间的设备，要建立对等体必须物理直连；同一AS之间，只要可以Ping通就可以建立对等体
